{% set blog_image = page.media.images|first %} 
        <section class="light-color ptb-160 bg-img" style="background-image: url('{{ blog_image.url }}');">
            <div class="container text-center">
                <div class="row">
                    <div class="col-md-12">
                        {{ content }}
                    </div>
                </div>
            </div>
        </section>