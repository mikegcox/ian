
{% set blog_image = page.media.images|first %}


<section class="">
    <div class="container ptb-60">
        <div class="row">
            <div class="col-md-6">{{ blog_image.cropResize(600, 600).html(page.title, page.title, '') }}</div>
            <div class="col-md-6">
            <h2>{{ page.title }}</h2>
            {{ content }}
            </div>
        </div>
    </div>
</section>

